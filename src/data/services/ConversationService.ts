import { injectable } from "inversify";
import IConversationService from "@src/core/services/IConversationService";

/**
 * Firbase conversation service
 *
 * @export
 * @class ConversationService
 * @implements {IConversationService}
 */
 @injectable()
export default class ConversationService implements IConversationService {

}